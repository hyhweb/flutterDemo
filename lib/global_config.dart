import 'package:flutter/material.dart';

class GlobalConfig {
  static bool dark = false;
  static ThemeData themeData = new ThemeData(primarySwatch: Colors.blue);
  static Color searchBackgroundColor = Colors.white;
  static Color cardBackgroundColor = new Color(0xFFFFFFF);
  static Color fontColor = Colors.black;
}